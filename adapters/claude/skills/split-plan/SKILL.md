---
name: split-plan
description: Split a reviewed feature plan into self-contained OG Relay stage briefs and machine-readable dependencies.
---

Read `@RELAY_ROOT@/docs/PLAN.md`, then execute its splitter protocol.
Write only plans/<slug>/. Use schema version 1, argv commands, serial stages and action phases.
Do not implement, branch, commit or launch a ship run.
